export {
	HandlebarAgent,
	type HandlebarAgentOpts,
	type RunCallOpts,
} from "./agent";
export { tool } from "./tool";
// Re-export core Handlebar so users don't have to install it separately
export { Handlebar } from "@handlebar/core";
